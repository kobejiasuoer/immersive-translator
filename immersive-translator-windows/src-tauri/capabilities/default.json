{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for app windows (panel etc.)",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:event:default",
    "global-shortcut:default"
  ]
}
